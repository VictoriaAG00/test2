body{
    cursor: none;
}

.cursor {

        width: 3rem;
        height: 3rem;
        border-radius: 50%;
        position: absolute;
        transform: translate(-50%, -50%);
        color: blueviolet;
        pointer-events: none;
        transition: all 0.3s ease;
        background: black;
        transition-property: background, transform;
        z-index: -1;
}

.link-grow{
    transform: scale(1.5);
    background: black;
}

.hover-link{
    color: white;

}
